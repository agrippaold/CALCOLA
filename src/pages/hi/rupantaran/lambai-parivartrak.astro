---
import { getCollection } from 'astro:content';
import ToolLayout from '../../../layouts/ToolLayout.astro';
import LengthConverter from '../../../components/calculators/conversion/LengthConverter';

const lang = 'hi';

const allTools = await getCollection('tools');
const toolEntry = allTools.find(
  (t) => t.data.id === 'length-converter' && t.id.startsWith('hi/')
);

if (!toolEntry) {
  return Astro.redirect('/hi/');
}

const tool = toolEntry.data;
---

<ToolLayout tool={tool} lang={lang}>
  <LengthConverter client:visible defaults={tool.defaults} lang={lang} />
</ToolLayout>

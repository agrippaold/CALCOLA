---
import { getCollection } from 'astro:content';
import ToolLayout from '../../../layouts/ToolLayout.astro';
import PasswordStrengthChecker from '../../../components/calculators/text/PasswordStrengthChecker';

const lang = 'hi';

const allTools = await getCollection('tools');
const toolEntry = allTools.find(
  (t) => t.data.id === 'password-strength-checker' && t.id.startsWith('hi/')
);

if (!toolEntry) {
  return Astro.redirect('/hi/');
}

const tool = toolEntry.data;
---

<ToolLayout tool={tool} lang={lang}>
  <PasswordStrengthChecker client:visible defaults={tool.defaults} lang={lang} />
</ToolLayout>

---
import { getCollection } from 'astro:content';
import ToolLayout from '../../../layouts/ToolLayout.astro';
import CompoundInterestCalculator from '../../../components/calculators/finance/CompoundInterestCalculator';

const lang = 'de';

const allTools = await getCollection('tools');
const toolEntry = allTools.find(
  (t) => t.data.id === 'compound-interest-calculator' && t.id.startsWith('de/')
);

if (!toolEntry) {
  return Astro.redirect('/de/');
}

const tool = toolEntry.data;
---

<ToolLayout tool={tool} lang={lang}>
  <CompoundInterestCalculator client:visible defaults={tool.defaults} lang={lang} />
</ToolLayout>

---
import BaseLayout from '../../../layouts/BaseLayout.astro';

const lang = 'pt-br';

const tools = [
  { name: 'Calculadora de Idade', slug: '/pt-br/data/calculadora-idade/', description: 'Calcule sua idade exata em anos, meses e dias a partir da data de nascimento.' },
  { name: 'Diferenca entre Datas', slug: '/pt-br/data/calculadora-diferenca-datas/', description: 'Encontre o tempo exato entre duas datas em anos, meses, semanas e dias.' },
];
---

<BaseLayout
  title="Calculadoras de Data - Idade, Diferenca entre Datas | CalcHub"
  description="Calculadoras de data gratuitas: calculadora de idade, diferenca entre datas. Calcule intervalos de tempo exatos instantaneamente."
  lang={lang}
>
  <section class="max-w-4xl mx-auto px-4 py-12">
    <nav class="text-sm text-gray-500 mb-6">
      <a href="/pt-br/" class="hover:text-blue-600">Inicio</a>
      <span class="mx-2">/</span>
      <span class="text-gray-900">Data e Hora</span>
    </nav>

    <h1 class="text-3xl font-bold text-gray-900 mb-4">Calculadoras de Data e Hora</h1>
    <p class="text-gray-600 mb-8 max-w-2xl">
      Ferramentas precisas para calculo de datas e tempo. Descubra sua idade exata e intervalos entre datas.
    </p>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
      {tools.map((tool) => (
        <a
          href={tool.slug}
          class="bg-white rounded-lg p-6 shadow-sm hover:shadow-md transition-shadow border border-gray-100"
        >
          <h2 class="font-semibold text-gray-900 text-lg mb-2">{tool.name}</h2>
          <p class="text-sm text-gray-500">{tool.description}</p>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>

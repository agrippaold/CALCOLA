---
import { getCollection } from 'astro:content';
import ToolLayout from '../../../layouts/ToolLayout.astro';
import OvulationCalculator from '../../../components/calculators/health/OvulationCalculator';

const lang = 'pt-br';

const allTools = await getCollection('tools');
const toolEntry = allTools.find(
  (t) => t.data.id === 'ovulation-calculator' && t.id.startsWith('pt-br/')
);

if (!toolEntry) {
  return Astro.redirect('/pt-br/');
}

const tool = toolEntry.data;
---

<ToolLayout tool={tool} lang={lang}>
  <OvulationCalculator client:visible defaults={tool.defaults} lang={lang} />
</ToolLayout>

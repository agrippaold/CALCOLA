---
import { getCollection } from 'astro:content';
import ToolLayout from '../../../layouts/ToolLayout.astro';
import WeightConverter from '../../../components/calculators/conversion/WeightConverter';

const lang = 'pt-br';

const allTools = await getCollection('tools');
const toolEntry = allTools.find(
  (t) => t.data.id === 'weight-converter' && t.id.startsWith('pt-br/')
);

if (!toolEntry) {
  return Astro.redirect('/pt-br/');
}

const tool = toolEntry.data;
---

<ToolLayout tool={tool} lang={lang}>
  <WeightConverter client:visible defaults={tool.defaults} lang={lang} />
</ToolLayout>

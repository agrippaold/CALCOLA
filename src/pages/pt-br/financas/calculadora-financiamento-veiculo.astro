---
import { getCollection } from 'astro:content';
import ToolLayout from '../../../layouts/ToolLayout.astro';
import AutoLoanCalculator from '../../../components/calculators/finance/AutoLoanCalculator';

const lang = 'pt-br';

const allTools = await getCollection('tools');
const toolEntry = allTools.find(
  (t) => t.data.id === 'auto-loan-calculator' && t.id.startsWith('pt-br/')
);

if (!toolEntry) {
  return Astro.redirect('/pt-br/');
}

const tool = toolEntry.data;
---

<ToolLayout tool={tool} lang={lang}>
  <AutoLoanCalculator client:visible defaults={tool.defaults} lang={lang} />
</ToolLayout>

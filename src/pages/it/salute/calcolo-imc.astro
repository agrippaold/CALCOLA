---
import { getCollection } from 'astro:content';
import ToolLayout from '../../../layouts/ToolLayout.astro';
import BMICalculator from '../../../components/calculators/health/BMICalculator';

const lang = 'it';

const allTools = await getCollection('tools');
const toolEntry = allTools.find(
  (t) => t.data.id === 'bmi-calculator' && t.id.startsWith('it/')
);

if (!toolEntry) {
  return Astro.redirect('/it/');
}

const tool = toolEntry.data;
---

<ToolLayout tool={tool} lang={lang}>
  <BMICalculator client:visible defaults={tool.defaults} lang={lang} />
</ToolLayout>

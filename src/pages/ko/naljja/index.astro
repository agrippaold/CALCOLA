---
import BaseLayout from '../../../layouts/BaseLayout.astro';

const lang = 'ko';

const tools = [
  { name: '나이 계산기', slug: '/ko/naljja/nai-gyesangi/', description: '생년월일로 정확한 나이를 년, 월, 일 단위로 계산하세요.' },
  { name: '날짜 차이 계산기', slug: '/ko/naljja/naljja-chai-gyesangi/', description: '두 날짜 사이의 일수, 주수, 월수를 계산하세요.' },
];
---

<BaseLayout
  title="날짜 계산기 - 나이, 날짜 차이 | CalcHub"
  description="무료 날짜 계산기: 나이 계산, 날짜 차이 계산. 빠르고 정확한 날짜 도구."
  lang={lang}
>
  <section class="max-w-4xl mx-auto px-4 py-12">
    <nav class="text-sm text-gray-500 mb-6">
      <a href="/ko/" class="hover:text-blue-600">홈</a>
      <span class="mx-2">/</span>
      <span class="text-gray-900">날짜 계산기</span>
    </nav>

    <h1 class="text-3xl font-bold text-gray-900 mb-4">날짜 계산기</h1>
    <p class="text-gray-600 mb-8 max-w-2xl">
      나이 계산, 날짜 차이 계산 등 날짜와 관련된 모든 계산을 쉽게 수행하세요.
    </p>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
      {tools.map((tool) => (
        <a
          href={tool.slug}
          class="bg-white rounded-lg p-6 shadow-sm hover:shadow-md transition-shadow border border-gray-100"
        >
          <h2 class="font-semibold text-gray-900 text-lg mb-2">{tool.name}</h2>
          <p class="text-sm text-gray-500">{tool.description}</p>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>

---
import BaseLayout from '../../../layouts/BaseLayout.astro';

const lang = 'ko';

const tools = [
  { name: '학점 계산기', slug: '/ko/gyoyuk/hakjeom-gyesangi/', description: '과목 학점과 성적으로 GPA를 계산하세요.' },
];
---

<BaseLayout
  title="교육 계산기 - 학점 계산 | CalcHub"
  description="무료 교육 계산기: GPA 학점 계산기. 과목별 성적으로 평균 학점을 계산하세요."
  lang={lang}
>
  <section class="max-w-4xl mx-auto px-4 py-12">
    <nav class="text-sm text-gray-500 mb-6">
      <a href="/ko/" class="hover:text-blue-600">홈</a>
      <span class="mx-2">/</span>
      <span class="text-gray-900">교육 계산기</span>
    </nav>

    <h1 class="text-3xl font-bold text-gray-900 mb-4">교육 계산기</h1>
    <p class="text-gray-600 mb-8 max-w-2xl">
      학점 계산 등 학업 계획에 도움이 되는 교육 도구를 제공합니다.
    </p>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
      {tools.map((tool) => (
        <a
          href={tool.slug}
          class="bg-white rounded-lg p-6 shadow-sm hover:shadow-md transition-shadow border border-gray-100"
        >
          <h2 class="font-semibold text-gray-900 text-lg mb-2">{tool.name}</h2>
          <p class="text-sm text-gray-500">{tool.description}</p>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>

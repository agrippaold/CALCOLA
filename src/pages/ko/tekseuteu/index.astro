---
import BaseLayout from '../../../layouts/BaseLayout.astro';

const lang = 'ko';

const tools = [
  { name: '단어 수 세기', slug: '/ko/tekseuteu/daneo-gyesu/', description: '텍스트의 단어 수, 문장 수, 단락 수를 세어보세요.' },
  { name: '글자 수 세기', slug: '/ko/tekseuteu/geulja-gyesu/', description: '텍스트의 글자 수와 공백 포함/제외 수를 확인하세요.' },
  { name: '비밀번호 생성기', slug: '/ko/tekseuteu/bimilbeonho-saengseong/', description: '사용자 설정에 따른 안전한 무작위 비밀번호를 생성하세요.' },
  { name: 'QR 코드 생성기', slug: '/ko/tekseuteu/qr-code-saengseong/', description: 'URL, 텍스트, 연락처 정보를 QR 코드로 변환하세요.' },
  { name: '비밀번호 강도 검사', slug: '/ko/tekseuteu/bimilbeonho-gangdo/', description: '비밀번호의 강도를 분석하고 개선 방법을 확인하세요.' },
];
---

<BaseLayout
  title="텍스트 도구 - 단어 수, 비밀번호 생성 | CalcHub"
  description="무료 텍스트 도구: 단어 수 세기, 글자 수 세기, 비밀번호 생성기, QR 코드 생성기."
  lang={lang}
>
  <section class="max-w-4xl mx-auto px-4 py-12">
    <nav class="text-sm text-gray-500 mb-6">
      <a href="/ko/" class="hover:text-blue-600">홈</a>
      <span class="mx-2">/</span>
      <span class="text-gray-900">텍스트 도구</span>
    </nav>

    <h1 class="text-3xl font-bold text-gray-900 mb-4">텍스트 도구</h1>
    <p class="text-gray-600 mb-8 max-w-2xl">
      단어 수 세기, 글자 수 세기, 비밀번호 생성, QR 코드 생성 등 텍스트 관련 유용한 도구를 제공합니다.
    </p>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
      {tools.map((tool) => (
        <a
          href={tool.slug}
          class="bg-white rounded-lg p-6 shadow-sm hover:shadow-md transition-shadow border border-gray-100"
        >
          <h2 class="font-semibold text-gray-900 text-lg mb-2">{tool.name}</h2>
          <p class="text-sm text-gray-500">{tool.description}</p>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>

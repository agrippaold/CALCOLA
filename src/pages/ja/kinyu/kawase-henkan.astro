---
import { getCollection } from 'astro:content';
import ToolLayout from '../../../layouts/ToolLayout.astro';
import CurrencyConverter from '../../../components/calculators/finance/CurrencyConverter';

const lang = 'ja';

const allTools = await getCollection('tools');
const toolEntry = allTools.find(
  (t) => t.data.id === 'currency-converter' && t.id.startsWith('ja/')
);

if (!toolEntry) {
  return Astro.redirect('/ja/');
}

const tool = toolEntry.data;
---

<ToolLayout tool={tool} lang={lang}>
  <CurrencyConverter client:visible defaults={tool.defaults} lang={lang} />
</ToolLayout>

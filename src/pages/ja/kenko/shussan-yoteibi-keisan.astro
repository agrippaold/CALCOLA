---
import { getCollection } from 'astro:content';
import ToolLayout from '../../../layouts/ToolLayout.astro';
import PregnancyCalculator from '../../../components/calculators/health/PregnancyCalculator';

const lang = 'ja';

const allTools = await getCollection('tools');
const toolEntry = allTools.find(
  (t) => t.data.id === 'pregnancy-due-date-calculator' && t.id.startsWith('ja/')
);

if (!toolEntry) {
  return Astro.redirect('/ja/');
}

const tool = toolEntry.data;
---

<ToolLayout tool={tool} lang={lang}>
  <PregnancyCalculator client:visible defaults={tool.defaults} lang={lang} />
</ToolLayout>

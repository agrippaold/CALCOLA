---
import BaseLayout from '../../../layouts/BaseLayout.astro';

const lang = 'ja';

const tools = [
  { name: '年齢計算', slug: '/ja/nichiji/nenrei-keisan/', description: '年・月・日で正確な年齢を計算し、今後の節目を表示します。' },
  { name: '日数計算', slug: '/ja/nichiji/nichisu-keisan/', description: '2つの日付間の正確な期間を年・月・週・日で計算します。' },
];
---

<BaseLayout
  title="日時計算ツール - 年齢・日数計算 | CalcHub"
  description="無料の日時計算ツール：年齢計算、日数計算。日付間の正確な期間を即座に計算します。"
  lang={lang}
>
  <section class="max-w-4xl mx-auto px-4 py-12">
    <nav class="text-sm text-gray-500 mb-6">
      <a href="/ja/" class="hover:text-blue-600">ホーム</a>
      <span class="mx-2">/</span>
      <span class="text-gray-900">日時計算ツール</span>
    </nav>

    <h1 class="text-3xl font-bold text-gray-900 mb-4">日時計算ツール</h1>
    <p class="text-gray-600 mb-8 max-w-2xl">
      正確な日付・時間の計算ツール。年齢や日付間の期間などを正確に算出します。
    </p>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
      {tools.map((tool) => (
        <a
          href={tool.slug}
          class="bg-white rounded-lg p-6 shadow-sm hover:shadow-md transition-shadow border border-gray-100"
        >
          <h2 class="font-semibold text-gray-900 text-lg mb-2">{tool.name}</h2>
          <p class="text-sm text-gray-500">{tool.description}</p>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>

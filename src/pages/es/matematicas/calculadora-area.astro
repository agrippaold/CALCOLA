---
import { getCollection } from 'astro:content';
import ToolLayout from '../../../layouts/ToolLayout.astro';
import AreaCalculator from '../../../components/calculators/math/AreaCalculator';

const lang = 'es';

const allTools = await getCollection('tools');
const toolEntry = allTools.find(
  (t) => t.data.id === 'area-calculator' && t.id.startsWith('es/')
);

if (!toolEntry) {
  return Astro.redirect('/es/');
}

const tool = toolEntry.data;
---

<ToolLayout tool={tool} lang={lang}>
  <AreaCalculator client:visible defaults={tool.defaults} lang={lang} />
</ToolLayout>

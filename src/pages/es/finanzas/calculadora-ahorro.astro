---
import { getCollection } from 'astro:content';
import ToolLayout from '../../../layouts/ToolLayout.astro';
import SavingsCalculator from '../../../components/calculators/finance/SavingsCalculator';

const lang = 'es';

const allTools = await getCollection('tools');
const toolEntry = allTools.find(
  (t) => t.data.id === 'savings-calculator' && t.id.startsWith('es/')
);

if (!toolEntry) {
  return Astro.redirect('/es/');
}

const tool = toolEntry.data;
---

<ToolLayout tool={tool} lang={lang}>
  <SavingsCalculator client:visible defaults={tool.defaults} lang={lang} />
</ToolLayout>

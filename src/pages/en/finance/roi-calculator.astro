---
import { getCollection } from 'astro:content';
import ToolLayout from '../../../layouts/ToolLayout.astro';
import ROICalculator from '../../../components/calculators/finance/ROICalculator';

const lang = 'en';

const allTools = await getCollection('tools');
const toolEntry = allTools.find(
  (t) => t.data.id === 'roi-calculator' && t.id.startsWith('en/')
);

if (!toolEntry) {
  return Astro.redirect('/en/');
}

const tool = toolEntry.data;
---

<ToolLayout tool={tool} lang={lang}>
  <ROICalculator client:visible defaults={tool.defaults} lang={lang} />
</ToolLayout>

---
import { getCollection } from 'astro:content';
import ToolLayout from '../../../layouts/ToolLayout.astro';
import MacroCalculator from '../../../components/calculators/health/MacroCalculator';

const lang = 'en';

const allTools = await getCollection('tools');
const toolEntry = allTools.find(
  (t) => t.data.id === 'macro-calculator' && t.id.startsWith('en/')
);

if (!toolEntry) {
  return Astro.redirect('/en/');
}

const tool = toolEntry.data;
---

<ToolLayout tool={tool} lang={lang}>
  <MacroCalculator client:visible defaults={tool.defaults} lang={lang} />
</ToolLayout>
